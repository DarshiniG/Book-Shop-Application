rules:
  - id: insecure-sql-injection
    patterns:
      - pattern: |
          $USER_INPUT = $_GET['user_id'];
      - pattern: |
          $sql = "... $USER_INPUT ...";
    message: "Potential SQL Injection: avoid using user input directly in SQL queries."
    languages: [php]
    severity: ERROR
